# Paper

Paper-related notes and manually distributable assets can be placed here when distribution permission is confirmed.

PDF files are ignored by default in `.gitignore`. If the author has permission to distribute a paper PDF, add it intentionally and document the permission status in this directory.
